class Tool {
	constructor(name, description) {
		this.name = name;
		this.description = description;
	}

	// listTool() {
	// 	$("#toolbox").append(`<p>${i}. ` + 
	// 		tool.name + 
	// 		`  (${tool.description})  ` +
	// 		`<button id="show-tool-${tool.id}">Show Tool</button></p>`
	// 	);
	// };
}



